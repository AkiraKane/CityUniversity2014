function Collect_Results()
% Import Data
X = [1 0;0 1;1 1;0 0];
Y=X;

X_target = [1 1 0 0]';
Y_target = X_target;

% Neural Network Parameters
Reg     = [1e-5 1e-3 0.01]; % Regularisation Value
epochs  = [30000 15000 5000];  % Number of Epochs to Train Network with
LR      = [0.9 0.5 0.1 0.01];  % Learning Rate value
Mo      = [0.9 0.5 0.1];  % Momentum
% Define Stopping Criteria.....TO DO

% Find all combinations and 
Combinations  = allcomb(Reg, epochs, LR, Mo);

% Results Table for Ranking - 6 Columns
%    Store Cost Of Validation
%    Accuracy of Validation
%    Regularisation
%    Epochs
%    Learning Rate
%    Momentum
Ranking_Table = zeros(size(Combinations, 1), 6); 

% Loop Through 
for 
Result_Line = Create_And_Train(X, X_target, Y, Y_target, epochs, LR, Mo, Reg)
    